# WorldBuilder ST Plugin Changelog

## 0.6.0

- **context_mode**: `truth` | `visibility` (default) | `belief`
- Project & simulation dropdowns; health check status
- Character card → entity mapping warning
- Simulation memory injection (`inject_memory`)
- ST writeback queue (`writeback_enabled`) — review in WB Simulator「ST 回写」tab
- Injection positions: `before_scenario`, `macro_only` (+ `{{worldbuilder_context}}` via `window.worldbuilder_get_context`)
- Last injection preview in settings drawer

## 0.2.0

- Initial graph-hop context injection
